# Copy to .env on the server. Never commit .env.
APP_URL=https://tripbook.pfeif.us
SESSION_SECRET=            # openssl rand -hex 32
POSTGRES_PASSWORD=         # openssl rand -hex 16
SETTINGS_SECRET=           # openssl rand -hex 32 (encrypts stored API keys)
AWS_REGION=us-east-1
S3_BUCKET=tripbook-media-416369639144
SES_FROM="Tripbook <tripbook@pfeif.us>"
ANTHROPIC_API_KEY=
# From deploy/provision.sh output (IAM user tripbook-app)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
ADMIN_EMAILS=david.pfeiffenberger@gmail.com
